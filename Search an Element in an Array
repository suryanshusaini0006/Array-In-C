#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    int i;
    int key;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
    scanf("%d",&arr[i]);
    }
    scanf("%d",&key);
    for(i=0;i<n;i++){
    if(arr[i]==key){
        printf("Element found at position %d",i+1);
        break;
    }
    }
    if(arr[i]!=key){
    printf("Element not found");
    }

    return 0;
}
